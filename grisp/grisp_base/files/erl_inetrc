%--- Erlang Inet Configuration -------------------------------------------------

% Add hosts
{host, {X,X,X,X}, ["your-pc"]}.

% Do not monitor the hosts file
{hosts_file, ""}.

% Disable cachingy
{cache_size, 0}.

% Specify lookup method
{lookup, [file, native]}.
