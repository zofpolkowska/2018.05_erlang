%--- Erlang Inet Configuration -------------------------------------------------

% Add hosts
{host, {192,168,1,3}, ["cactus"]}.

% Do not monitor the hosts file
{hosts_file, ""}.

% Disable cachingy
{cache_size, 0}.

% Specify lookup method
{lookup, [file, dns]}.
